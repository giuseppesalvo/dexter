{
  "name": "dexter",
  "version": "0.0.1",
  "description": "nodejs framework for making bots",
  "main": "dist/index.js",
  "scripts": {
    "watch": "npx tsc -p ./ -w",
    "build": "npx tsc -p ./",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build"
  },
  "author": "Giuseppe Salvo",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^9.4.7",
    "telegraf": "^3.19.0"
  },
  "devDependencies": {
    "typescript": "^2.7.2"
  },
  "engines": {
    "node": "8.6.0",
    "npm": "5.5.1"
  }
}
